-- V5 intentionally left blank; audit tables are created lazily by Envers.


-- V5__envers_audit_tables.sql
-- Envers creates the _aud tables automatically the first time an entity changes.
-- Nothing technical is needed here, but you may want to record the version step:
--SELECT 1;
